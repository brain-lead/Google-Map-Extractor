# 🧠 BrainLead Google Map Extractor

> **Turn any Google Maps search into a clean, scored, export-ready lead list — in one click.**

![BrainLead Google Map Extractor](image.png)

**BrainLead Google Map Extractor** is a desktop lead-generation engine that scrapes Google Maps, enriches every business with deep contact data, scores your hottest leads, and exports them — no coding, no browser extensions, no manual copy-paste.

---

## 🚀 Why BrainLead Google Map Extractor?

| Without BrainLead Google Map Extractor | With BrainLead Google Map Extractor |
| -------------------------------------- | ----------------------------------- |
| Copy-pasting results by hand | One click → structured table → export |
| No idea which leads are worth calling | Automatic **lead scoring** ranks the best fits first |
| Thin contact info on Google | Emails, phones, websites, socials, hours, menus, reviews |
| Slow, one city at a time | **Multi-city & area-grid scans** run every location automatically |
| Proxy bans slow you down | Built-in **proxy pool** with per-request rotation |

---

## ✨ Features — Everything BrainLead Google Map Extractor Does

### 🔎 Search & Coverage
1. **Keyword + Location search** — e.g. `plumber` in `New York`, or batch `Restaurants + New York, Los Angeles, Chicago`.
2. **Multi-city batch scans** — separate keywords or cities with commas / newlines; BrainLead Google Map Extractor scans **every city automatically**.
3. **Category autocomplete** — smart suggestions as you type.
4. **Area Scan (draw a region)** — drop a polygon on the map and BrainLead Google Map Extractor tiles it automatically.
5. **Coordinate-grid coverage** — the area is split into a grid so no part is missed.
6. **Tile preview** — see exactly how many tiles will be searched *before* you run.
7. **Automatic de-duplication** — duplicate businesses across tiles and cities are merged.

### 📞 Deep Enrichment (per business)
8. **Phone numbers**
9. **Websites**
10. **Email addresses** (extracted and validated)
11. **Social profiles** — Facebook, Instagram, X (Twitter), LinkedIn, YouTube
12. **Opening hours**
13. **Menu / services**
14. **Star rating**
15. **Review counts**
16. **Review text mining**
17. **Business categories / type**
18. **Price level**
19. **Plus code / geo-coordinates**
20. **Full formatted address**

### 🔥 Lead Intelligence
21. **Automatic lead scoring** (Hot / Warm) with adjustable weights.
22. **Filter: Has email**
23. **Filter: Has social**
24. **Filter: Has website**
25. **Filter: Has phone**
26. **Filter: Minimum rating**
27. **Filter: Minimum reviews**
28. **Filter: Hot leads only**
29. **Country facet + saved filter presets**
30. **Area / location facet**

### ⚡ Speed & Infrastructure
31. **Concurrent threads (1–16)** to control search + enrichment speed.
32. **Built-in proxy pool** — bulk upload and per-request rotation.
33. **Cookie session** capture / import / auto-generate to stay under limits.
34. **Browser impersonation** to avoid bot detection.
35. **Automatic market detection** — the right country/language is set from your area or selected country.

### 🗂️ Jobs & Output
36. **Job history** with re-score and compare.
37. **Export to CSV, JSON, and Excel (XLSX)**.
38. **Shareable report view**.
39. **In-app activation** — free tier (limited / search) with unlimited upgrade.
40. **Settings persistence** — threads, country, and max results are saved.

---

## 🖥️ Quick Start (Windows)

1. Download / locate **`BrainLead.exe`** (a single file).
2. **Double-click** it. A local server starts and your browser opens to the app.
3. On first run, activate via the in-app **Activate** button (free tier is limited to 50 results / search).
4. Enter a **Keyword**, a **Location**, set **Concurrent threads**, and hit **New Extraction**.

> 💡 **Heads-up:** Windows Defender / some antivirus tools may flag `BrainLead.exe` as a false positive — it is a packed single-file app. Add an exclusion for the file (or its folder) if it is blocked or quietly deleted.

---

## 🎯 How It Works

1. **Search** — BrainLead Google Map Extractor queries Google Maps for your keyword/location (or tiles an area).
2. **Enrich** — each business is fetched in parallel to pull deep contact + social data.
3. **Score** — leads are ranked so the highest-potential businesses float to the top.
4. **Export** — filter by any signal and save the list.

Results are sorted **Lead score: high → low**. Each result card shows name, category, rating, review count, address, phone, website, emails, social links, and a **lead-score badge**.

---

## 🌍 Multi-City & Area Scans

BrainLead Google Map Extractor is built for **scale**:

- Separate keywords or cities with commas / newlines — `Restaurants + New York, Los Angeles, Chicago` scans **every city automatically**.
- **Area scan** mode tiles a coordinate grid over any region you draw, searches each tile, and merges + de-duplicates results.
- The country/market is detected automatically from the drawn area or your selection — you never configure regions manually.

---

## 🔐 Data & Privacy

BrainLead Google Map Extractor is a **local desktop tool** — everything runs on *your* machine. Your data lives in your user profile and survives restarts:

```
C:\Users\<you>\AppData\Roaming\BrainLead\
├── data\            # job history, settings, saved session
├── stdout.log       # startup output
└── stderr.log       # errors (check this first if something fails)
```

To start completely fresh, close the app and delete the `BrainLead` folder under `%APPDATA%`.

---

## 🤝 Need a Custom Tool, Script, or Software?

**BrainLead builds custom tools, automation scripts, and software for businesses.**

If you need a lead generator, a data pipeline, a scraper, a dashboard, or any bespoke software — **we can build it for you.**

- 🐦 **@BrainLead**
- 🤖 **@brainleadbot**
- ✉️ **brain_lead@outlook.com**

© BrainLead. All rights reserved.
